# 个人日程记录表单提交系统需求规格说明书（V2.1）
---

## 一、项目概述 
本项目旨在开发一款基于 Next.js+React 框架的 Web 端个人日程记录系统，核心功能为支持用户通过表单（含语音识别）提交多维度日程信息，并通过全局可视化仪表盘展示日程数据的各类统计维度（优先级分布、类别占比、时间趋势等），帮助用户高效管理、分析个人与工作日程，提升时间管理效率。  

**补充说明：**
- **目标用户**：职场人士、学生及需要高效管理时间的个人用户，核心需求为“清晰记录-智能分析-高效执行”的闭环时间管理。
- **问题痛点**：解决传统日程工具“记录繁琐、分析缺失、提醒不智能”的问题，通过语音录入降低记录门槛，通过多维度图表提升数据洞察能力。
- **核心价值**：实现“低成本记录 + 高价值分析”，让用户通过数据驱动优化时间分配。


## 二、核心业务需求 
### 2.1 日程记录表单提交 
#### 2.1.1 表单核心字段 
| 字段名称 | 字段类型 | 字段说明 | 约束条件 |
|---------|---------|---------|---------|
| 日程标题 | 文本输入框 | 简洁描述日程核心内容（如 “周一项目例会”） | 必填，长度 1-50 字符 |
| 日程详情 | 多行文本框 | 详细描述日程内容（如会议议题、待办事项等） | 可选，长度 0-500 字符 |
| 优先级 | 下拉选择 / 单选框 | 分为高、中、低三个等级 | 必填，默认 “中” |
| 类别 | 下拉选择 / 单选框 | 分为 “工作”“个人日常” 两大类；支持扩展子类别（如工作 - 会议、个人 - 生活缴费） | 必填，默认 “个人日常” |
| 开始时间 | 日期时间选择器 | 日程开始的具体时间 | 必填，格式为 YYYY-MM-DD HH:mm；支持与当前时间关联（如“今天下午 3 点”） |
| 结束时间 | 日期时间选择器 | 日程结束的具体时间 | 必填，需晚于开始时间；支持智能填充（如开始时间 +1 小时） |
| 提醒设置 | 复选框 / 下拉框 | 支持设置提前 5/15/30 分钟提醒，可多选 | 可选，默认不提醒 |
| 完成状态 | 单选框 | 未完成、已完成 | 必填，默认 “未完成” |
| 备注 | 多行文本框 | 额外补充信息 | 可选，长度 0-200 字符 |

**补充约束：**
- 时间格式严格校验，支持手动输入与日历选择双重交互；
- 子类别扩展规则：允许用户自定义子类别（需经表单校验，长度 1-20 字符），并支持分类管理（编辑/删除子类别）；
- 重叠日程提示：若新增日程与现有日程时间重叠，给出警告提示（非强制阻止，允许用户确认后提交）。

#### 2.1.2 表单操作逻辑 
- **提交**：表单字段校验通过后，将数据提交至后端存储，提交成功后清空表单（可选保留“优先级”“类别”“完成状态”等基础字段）并给出成功提示，同时触发仪表盘数据刷新；
- **暂存**：支持将未完成的表单数据暂存为草稿（自动保存至 LocalStorage，最多保留 10 条草稿），返回时可加载草稿内容（支持选择历史草稿）；
- **重置**：清空当前表单所有已填内容，需二次确认（模态框提示“确定要清空所有内容吗？”）；
- **回显**：编辑已有日程时，自动回显所有字段内容，包括自定义子类别；
- **版本管理**：编辑日程时，保留历史版本记录（最多 5 个版本），支持查看历史变更。

### 2.2 语音识别记录 
#### 2.2.1 核心能力 
- **语音录入触发**：提供 “按住说话”“点击开始 / 结束” 两种触发方式，支持中文普通话识别；
- **语音转文字**：将识别后的文字自动填充至表单字段，规则如下：
  - 核心语义（如 “周三下午 3 点和客户沟通”）填充至 “日程标题”；
  - 详细描述（如 “沟通产品需求，准备演示文档”）填充至 “日程详情”；
  - 识别到 “紧急”“重要” 等关键词自动标记为 “高优先级”，识别到 “普通”“常规” 标记为 “中 / 低优先级”；
  - 识别到 “上班”“会议”“汇报” 等关键词自动归类为 “工作”，识别到 “吃饭”“购物”“休息” 等归类为 “个人日常”；
  - 识别到时间信息（如 “明天上午 9 点到 10 点”）自动填充至开始 / 结束时间；
  - 识别到 “已完成”“搞定” 等关键词自动标记 “完成状态” 为 “已完成”；
- **语音交互反馈**：识别中显示加载动画（如波形图），识别失败给出 “请重试” 提示，识别成功给出文字预览并支持手动修改；
- **兼容性**：支持 PC 端（麦克风权限）、移动端（浏览器麦克风权限）的语音识别。

**补充说明：**
- **错误处理**：
  - 麦克风权限被拒绝时，显示引导文案（“请开启麦克风权限以使用语音录入”）并提供设置入口；
  - 识别超时（超过 10 秒无语音输入）自动停止录制并提示；
- **多轮语音录入**：支持分段录制，自动拼接识别结果（如先录标题，再录详情）；
- **语言扩展**：预留多语言支持接口（如英文、粤语），便于后续扩展。

#### 2.2.2 体验优化 
- 支持语音打断：录制过程中可随时停止（松开按住按钮或点击结束按钮），避免无效录音；
- 文字修正：识别结果填充后，允许用户手动编辑各字段内容（支持快捷键操作）；
- 历史语音记录：可选保存语音转文字的历史记录（仅保存文字结果，不存储音频文件），便于追溯（最多保留 50 条）；
- **智能纠错**：针对常见识别错误（如谐音字）提供自动修正建议，用户可选择接受或拒绝。

### 2.3 日程管理基础功能 
#### 2.3.1 列表展示 
- 按时间倒序展示所有日程，支持按 “优先级”“类别”“完成状态” 筛选；
- 列表项展示核心信息：标题、时间、优先级、类别、完成状态，高优先级项标红 / 加粗，已完成项划灰 / 加删除线；
- 支持分页（默认每页 20 条）或滚动加载（用户可切换模式）；
- 支持 “标记为已完成 / 未完成” 快捷操作，无需进入编辑页。

**补充功能：**
- **排序选项**：支持按 “开始时间”“优先级”“类别” 升序 / 降序排序；
- **视图切换**：支持 “列表视图”“日历视图” 切换，日历视图直观展示每日日程分布；
- **快捷筛选**：顶部提供常用筛选快捷入口（如“今天的日程”“高优先级未完成”）。

#### 2.3.2 编辑 / 删除 / 详情 
- 每条日程提供编辑、删除、查看详情操作按钮；
- 删除需二次确认（模态框提示“确定要删除该日程吗？此操作不可恢复”），编辑时回显原数据至表单，详情页展示完整字段信息；
- 批量操作：支持批量删除、批量标记完成状态（通过复选框选择，支持“全选/取消全选”）。

**补充说明：**
- **详情页交互**：支持从详情页直接跳转到编辑页，删除操作后返回列表页；
- **批量操作反馈**：批量操作完成后，给出明确提示（如“成功删除 3 条日程”）。

#### 2.3.3 搜索 
- 支持按 “日程标题 / 详情 / 备注” 关键词模糊搜索，支持按时间范围（近 7 天 / 近 30 天 / 自定义）筛选；

**补充功能：**
- **高级搜索**：支持组合筛选（如“高优先级 + 工作类别 + 近 7 天”）；
- **搜索历史**：保存最近 10 条搜索记录，支持快速复用；
- **搜索结果高亮**：匹配关键词在搜索结果中高亮显示。

### 2.4 全局可视化仪表盘（核心新增） 
#### 2.4.1 仪表盘布局 
- 首页默认展示仪表盘（可在用户设置中配置是否默认显示列表），分为 “核心数据概览区”“图表分析区”“快捷操作区”；
- 支持仪表盘 / 列表视图一键切换（顶部导航栏或侧边栏提供切换按钮），支持自定义图表展示顺序（拖拽调整位置）。

**补充说明：**
- **响应式布局**：
  - PC 端（≥1200px）：三栏布局（概览区 + 图表区 + 快捷操作区）；
  - 平板端（768-1199px）：两栏布局（概览区 + 图表区），快捷操作区折叠为悬浮按钮；
  - 移动端（<768px）：单栏滚动布局，图表自适应缩放，支持手势滑动切换。

#### 2.4.2 核心数据概览（卡片式展示） 
| 指标名称 | 展示方式 | 说明 |
|---------|---------|---------|
| 总日程数 | 数字卡片 | 统计所有状态的日程总数 |
| 待完成日程数 | 数字卡片（标红） | 未完成状态的日程数 |
| 高优先级日程数 | 数字卡片 | 优先级为 “高” 的日程数 |
| 本周新增日程数 | 数字卡片 | 近 7 天新增的日程数量 |
| 工作 / 个人日程占比 | 环形迷你图表 | 直观展示工作、个人日常的数量占比 |

**补充指标：**
- **完成率**：当日/本周已完成日程占比（如“今日完成率 60%”）；
- **平均日程时长**：统计所有日程的平均持续时间（如“平均 45 分钟/个”）。

#### 2.4.3 图表分析区（支持筛选时间维度：近 7 天 / 近 30 天 / 近 3 个月 / 全年 / 自定义） 
| 图表类型 | 展示维度 | 交互能力 |
|---------|---------|---------|
| 环形图 / 饼图 | 1. 日程优先级分布（高 / 中 / 低占比）<br>2. 日程类别分布（工作 / 个人 + 子类别）<br>3. 完成状态分布（已完成 / 未完成） | 点击图例可筛选，悬停显示具体数值 / 占比 |
| 柱状图 / 条形图 | 1. 每日 / 每周日程数量趋势（按天 / 周统计新增数）<br>2. 工作 / 个人日程每日分布对比 | 支持时间轴缩放，悬停显示具体日期 / 数量 |
| 折线图 | 1. 完成率趋势（每日 / 每周已完成日程占比）<br>2. 高优先级日程数量趋势 | 支持切换时间粒度，显示平均值 / 最大值标线 |
| 堆叠柱状图 | 按类别 + 优先级组合统计（如工作 - 高优先级、个人 - 中优先级等） | 点击堆叠块可下钻查看对应日程列表 |
| 日历热力图 | 按日期展示日程密集度（颜色越深表示当日日程越多） | 点击日期可查看当日所有日程，支持跳转到列表 |

**补充说明：**
- **图表默认配置**：
  - 默认时间范围为“近 7 天”；
  - 图表配色统一（如工作用蓝色系，个人用绿色系，高优先级用红色系）；
  - 无数据时显示“暂无数据”占位图（含引导文案“快去添加第一条日程吧！”）。
- **新增图表类型**：
  - **雷达图**：多维度对比（如工作/个人、高/中/低优先级的时间分配）；
  - **漏斗图**：日程完成转化漏斗（如“创建 → 开始 → 完成”的转化率）。

#### 2.4.4 交互能力 
- 时间筛选：所有图表支持统一的时间维度筛选（近 7 天 / 30 天 / 自定义），筛选后所有图表实时刷新；
- 图表联动：点击图表中的维度（如 “高优先级”），自动筛选下方日程列表展示对应数据；
- 数据导出：支持将图表数据导出为 Excel/PNG 图片（可选）；
- 刷新机制：新增 / 编辑 / 删除日程后，仪表盘数据自动刷新（无需手动刷新页面）；
- 自定义展示：支持隐藏 / 显示指定图表，保存用户自定义布局（本地存储）。

**补充功能：**
- **图表钻取**：支持从汇总图表钻取到明细数据（如从“月度趋势”钻取到“每日详情”）；
- **数据对比**：支持本周 vs 上周、本月 vs 上月数据对比（图表中用不同颜色标注差异）；
- **分享功能**：支持将图表截图或数据链接分享给他人（需用户确认）。

#### 2.4.5 快捷操作区 
- 仪表盘页面提供 “新增日程”“批量处理”“数据导出” 等快捷按钮；
- 展示 “即将到期日程”（近 24 小时内开始的未完成日程）提醒卡片，点击可直接编辑。

**补充功能：**
- **智能推荐**：根据历史数据推荐“今日优先完成”的日程（如高优先级 + 即将到期）；
- **快捷筛选入口**：提供常用筛选条件的快捷入口（如“查看所有未完成高优先级日程”）。


## 三、非功能需求 
### 3.1 技术栈要求 
- 前端框架：Next.js（≥14.x）+ React（≥18.x）；
- 状态管理：React Context + useReducer 或 Redux Toolkit（需管理仪表盘筛选状态、图表数据）；
- UI 组件库：Ant Design（推荐，含丰富图表组件）、MUI 或 Tailwind CSS + shadcn/ui；
- 图表库：ECharts（推荐）、Recharts 或 Ant Design Charts；
- 语音识别：集成第三方 SDK（百度 / 阿里云 / 讯飞语音识别）或浏览器原生 Web Speech API；
- 数据存储：
  - 前端：LocalStorage（草稿 / 用户配置）、SessionStorage（临时状态）、IndexedDB（可选，用于存储大量历史数据）；
  - 后端：对接 RESTful API/GraphQL，支持数据持久化，需提供图表统计接口（按维度聚合数据）；
- 数据处理：前端支持简单的本地数据聚合（开发阶段），生产环境优先调用后端聚合接口。

**补充说明：**
- **技术选型原则**：优先选择社区活跃度高、文档完善、性能优异的框架/库；
- **可扩展性**：预留第三方集成接口（如日历同步、消息通知）。

### 3.2 性能要求 
- 页面加载时间≤2s（仪表盘首次加载可放宽至 3s，需加骨架屏）；
- 表单提交 / 编辑 / 删除响应时间≤1s；
- 语音识别转文字延迟≤3s（网络正常）；
- 图表渲染 / 筛选刷新时间≤1s；
- 兼容主流浏览器：Chrome（≥90）、Firefox（≥88）、Safari（≥14）、Edge（≥90）；
- 移动端适配：仪表盘图表自适应缩放，支持触屏手势（如双指缩放热力图）。

**补充要求：**
- **并发支持**：单实例支持≥1000 并发用户访问；
- **离线支持**：断网情况下，支持查看已缓存的日程数据和仪表盘，联网后自动同步；
- **资源优化**：图片、音频等静态资源压缩处理，CDN 加速分发。

### 3.3 交互体验 
- 表单校验：实时反馈（失去焦点校验），错误提示清晰（红色文字 + 图标）；
- 语音交互：录制按钮区分 “未录制 / 录制中（动画）/ 录制完成” 状态，识别失败给出重试按钮；
- 图表交互：
  - 加载中显示骨架屏 / 加载动画，无数据时显示 “暂无数据” 占位图；
  - 悬停提示框样式统一，包含 “数值、占比、时间” 等核心信息；
  - 筛选操作有加载反馈，避免用户重复点击；
- 全局反馈：操作成功 / 失败给出 Toast 提示，重要操作（删除 / 批量修改）给出模态框确认；
- 响应式：适配 PC（≥1200px）、平板（768-1199px）、手机（<768px），移动端仪表盘自动折叠部分非核心图表，支持滑动查看。

**补充要求：**
- **可访问性**：符合 WCAG 2.1  AA 级标准，支持键盘导航、屏幕阅读器；
- **国际化**：支持多语言切换（中文简体 / 英文）；
- **主题切换**：支持浅色 / 深色主题切换，尊重系统默认主题。

### 3.4 数据安全与隐私 
- 本地暂存的草稿 / 配置数据加密存储（如 Base64 + 简单加密）；
- 语音识别过程中仅传输文字结果，不存储原始语音文件（用户授权除外）；
- 表单 / 图表数据传输采用 HTTPS，避免明文传输；
- 支持数据清空功能，用户可一键删除本地所有缓存数据；
- 仪表盘数据仅展示当前用户自身数据，无权限越界风险。

**补充说明：**
- **数据加密**：后端存储的数据需加密（如 AES-256），密钥定期轮换；
- **隐私政策**：明确告知用户数据收集、使用、存储的规则，遵循 GDPR / CCPA 等隐私法规；
- **权限控制**：实现基于角色的访问控制（RBAC），预留扩展到团队版的权限架构。

### 3.5 可维护性 
- 图表组件封装为通用组件，支持复用（如通用饼图、柱状图组件）；
- 语音识别逻辑解耦，便于切换第三方 SDK；
- 数据请求层封装，统一处理接口异常、加载状态；
- 代码规范：遵循 ESLint+Prettier，注释覆盖率≥60%（核心业务逻辑）。

**补充要求：**
- **文档完备**：提供前端架构文档、API 文档、部署文档；
- **测试覆盖率**：单元测试覆盖率≥80%，集成测试覆盖率≥60%；
- **CI/CD**：搭建持续集成 / 持续部署流程，支持自动化测试和部署。


## 四、页面结构规划 
### 4.1 核心页面 
- 全局仪表盘页：核心数据概览、多维度图表、快捷操作、即将到期提醒；
- 日程列表页：完整日程列表、筛选 / 搜索 / 批量操作、新增按钮；
- 日程新增 / 编辑页：含完整表单 + 语音录入功能；
- 日程详情页：展示单条日程完整信息，支持编辑 / 删除操作。

**补充页面：**
- **用户设置页**：配置默认视图、主题、语言、数据备份/导入等；
- **帮助中心页**：提供功能说明、常见问题解答、视频教程链接；
- **404 错误页**：资源不存在时的友好提示；
- **系统错误页**：服务器错误或网络异常时的提示，含重试按钮。

### 4.2 页面交互流程 
- 仪表盘页 → 点击 “新增日程” → 跳转到新增表单页（语音录入）→ 提交后返回仪表盘并自动刷新数据；
- 仪表盘页 → 点击图表维度（如 “高优先级”）→ 自动跳转到列表页并筛选对应数据；
- 仪表盘页 / 列表页 → 点击 “编辑” → 跳转到编辑表单页 → 提交后返回原页面并刷新；
- 列表页 → 点击 “仪表盘” 按钮 → 切换到仪表盘视图。

**补充交互：**
- **导航设计**：
  - 顶部导航栏：包含 Logo、页面切换按钮（仪表盘 / 列表）、搜索框、用户头像（下拉菜单含设置、帮助、退出）；
  - 侧边栏（可选）：针对 PC 端，提供完整的页面导航菜单；
- **面包屑导航**：在编辑 / 详情页显示当前位置（如“仪表盘 > 日程列表 > 编辑日程”）；
- **模态框与抽屉**：编辑 / 详情页可通过模态框或抽屉实现（减少页面跳转），提升交互流畅度。


## 五、扩展需求（可选） 
### 5.1 优先级分类 
- **P0（核心扩展）**：
  - 外部日历集成（Google Calendar、Outlook、苹果日历）：支持双向同步；
  - 数据备份 / 导入：支持将日程数据导出为 JSON/Excel，支持导入备份数据。
- **P1（重要扩展）**：
  - 自定义图表：允许用户新增自定义图表（如按提醒设置统计、按耗时统计）；
  - 多维度对比：支持本周 / 上周、本月 / 上月数据对比（图表标注差异）。
- **P2（可选扩展）**：
  - 目标设置：支持设置 “每日完成率目标”，仪表盘展示目标达成情况；
  - 语音播报：支持仪表盘核心数据、待完成日程语音播报；
- **P3（远期规划）**：
  - 团队版：支持多用户协作，仪表盘增加团队维度统计（需后端支持）；
  - AI 智能建议：基于历史数据，AI 推荐最优时间分配方案。


## 六、验收标准 
### 6.1 功能验收 
- 表单：所有字段校验逻辑生效，语音识别可按规则填充字段，提交 / 暂存 / 编辑功能正常；
- 日程管理：列表展示 / 筛选 / 搜索 / 批量操作 / 详情查看功能正常，数据同步准确；
- 仪表盘：
  - 核心数据概览实时更新，数值准确；
  - 所有图表可正常渲染，时间维度筛选生效，交互（悬停 / 点击 / 联动）正常；
  - 新增 / 删除 / 编辑日程后，仪表盘数据自动刷新，无延迟 / 数据错误；
  - 图表下钻、联动列表功能正常。

**补充验收点：**
- **语音识别**：在安静环境下，中文普通话识别准确率≥95%；
- **自定义子类别**：支持新增、编辑、删除子类别，且在表单中正常显示；
- **数据导出**：导出的 Excel/PNG 文件格式正确，数据完整。

### 6.2 性能验收 
- 页面加载、表单提交、图表刷新响应时间符合性能要求；
- 移动端适配无样式错乱，触屏交互流畅；
- 并发操作（如快速新增 + 筛选图表）无数据异常 / 页面崩溃。

**补充验收点：**
- **可访问性测试**：通过 WCAG 2.1 AA 级标准测试；
- **兼容性测试**：在指定浏览器版本中功能正常，无样式差异；
- **离线测试**：断网情况下可查看缓存数据，联网后自动同步。

### 6.3 体验验收 
- 操作反馈清晰，无无提示 / 错误提示不明确的情况；
- 图表交互符合用户直觉，无操作逻辑混乱；
- 语音识别成功率≥90%（标准普通话、无背景噪音环境）。

**补充验收点：**
- **用户满意度**：邀请 10 名目标用户进行 UAT 测试，满意度评分≥4.5（满分 5 分）；
- **易用性测试**：新用户可在 5 分钟内完成核心操作（新增日程、查看仪表盘）；
- **视觉一致性**：UI 设计符合设计规范，配色、字体、间距统一。


## 七、项目管理与交付 
### 7.1 开发阶段划分 
- **需求分析与设计**：2 周（完成 PRD 细化、UI/UX 设计、技术方案设计）；
- **前端开发**：8 周（完成核心功能开发、测试、优化）；
- **后端对接与集成测试**：2 周（与后端 API 对接，完成集成测试）；
- **UAT 测试与优化**：2 周（用户验收测试，bug 修复，性能优化）；
- **部署与上线**：1 周（生产环境部署，监控搭建）。

### 7.2 交付物 
- 前端代码库（含完整文档）；
- 测试报告（单元测试、集成测试、UAT 测试）；
- 部署文档；
- 操作手册（用户使用指南）。


## 八、风险评估与应对 
| 风险项 | 影响程度 | 发生概率 | 应对措施 |
|-------|---------|---------|---------|
| 语音识别准确率低 | 中 | 中 | 集成多家语音识别 SDK，支持用户切换；优化识别算法，提供手动纠错功能 |
| 图表数据量大导致性能问题 | 高 | 中 | 实现数据分页加载；优化图表渲染逻辑，使用虚拟滚动；后端提供聚合数据接口 |
| 跨浏览器兼容性问题 | 中 | 高 | 制定浏览器兼容性测试用例，使用自动化测试工具（如 Cypress）进行测试；针对不同浏览器做适配 |
| 数据安全漏洞 | 高 | 低 | 定期进行安全审计；遵循安全编码规范；使用成熟的加密算法和框架 |


---

**版本历史：**
- V2.0：初始版本，定义核心功能和技术栈；
- V2.1：补充完善，增强细节描述，扩展功能边界，明确非功能需求和验收标准。